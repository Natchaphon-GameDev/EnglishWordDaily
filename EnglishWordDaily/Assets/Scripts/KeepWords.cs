using System.Collections;using System.Collections.Generic;using TMPro;using UnityEngine;using UnityEngine.UI;public class KeepWords : MonoBehaviour{   private Dictionary<string,string> words;   [SerializeField] private InputField inputWordEnter;   [SerializeField] private InputField inputWordSearch;   [SerializeField] private TextMeshProUGUI simpleText;   private void WordCategorySeparate()   {      switch (inputWordEnter.text[0].ToString())      {         case "A" :            words.Add("A",inputWordEnter.text);            break;         case "B" :            words.Add("B",inputWordEnter.text);            break;         case "C" :            words.Add("C",inputWordEnter.text);            break;         case "D" :            words.Add("D",inputWordEnter.text);            break;         case "E" :            words.Add("E",inputWordEnter.text);            break;      }      var a = inputWordEnter.text[0];   }   public void ShowWords()   {      foreach (var word in words)      {      }   }}